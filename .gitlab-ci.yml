stages:
  - build
  - deploy


image: google/cloud-sdk:latest


before_script:
  - gcloud auth activate-service-account --key-file "$GCLUD_SERVICE_ACCOUNT_KEY"
  - gcloud projects set $GCP_PROJECT_ID


build-job:
  stage: build
  only: 
    - main
  script:
    - echo "Hello"


deploy-job:
  stage: deploy
  only:
    - main
  script:
    - echo "Hello"


after_script:
  - rm /tmp/service-account-key.json