options:
    logging: CLOUD_LOGGING_ONLY

steps:
    # Docker Build
    - name: "gcr.io/cloud-builders/docker"
      args: ["build", "-t", "asia-southeast1-docker.pkg.dev/$PROJECT_ID/git-gram/git-gram-backend:$SHORT_SHA", "."]

# Push to Artifact Registry
images:
  - "asia-southeast1-docker.pkg.dev/$PROJECT_ID/git-gram/git-gram-backend:$SHORT_SHA"
